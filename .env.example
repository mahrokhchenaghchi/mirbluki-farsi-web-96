# Local / test mode (default when Supabase is not configured)
VITE_JOMA_MODE=local

# Production only — do not set these for local testing
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
