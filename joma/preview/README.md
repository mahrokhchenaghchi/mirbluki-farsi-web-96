# پیش‌نمایش نسخه PHP جوما

این پوشه فقط برای تست محلی / Arena است. روی cPanel به آن نیازی نیست.

سرور، **همان فایل‌های PHP واقعی** را اجرا می‌کند (نه نسخه React). ذخیره روی فایل است و به MySQL واقعی وصل نمی‌شود.
