<?php
/**
 * JOMA — تنظیمات «جوجهٔ من» (B5 — مرحلهٔ ۱)
 *
 * این فایل فقط «آستانه»ها را نگه می‌دارد، نه داده. هیچ عددی از اینجا
 * به‌عنوان دادهٔ کاربر گزارش نمی‌شود؛ فقط مرز مرحله‌ها را تعیین می‌کند.
 *
 * تصمیم مالک (B5): جوجه با سه سنجهٔ موجود شروع می‌شود
 *   🌾 ظرف دونه  ← ثبت معتبر کارها
 *   💧 ظرف آب    ← ثبت ACT002 (هر ثبت موجود = قطعی)
 *   🏠 خونهٔ تمیز ← بودن رکورد حال روز
 * سنجهٔ چهارم («وقت حمام») تا ساخته‌شدن منبعش نمایش داده نمی‌شود.
 *
 * اعداد زیر «پیش‌فرض» است و مالک می‌تواند آن‌ها را تغییر دهد.
 * واحد «دونه» = تعداد ثبت‌های معتبر عملکرد (طبق داستان جوجه: با هر ثبت معتبر، یک دونه).
 */
if (!isset($JOOJE_CONFIG)) $JOOJE_CONFIG = array();

// خاموش‌کردن کامل قابلیت (اضطراری): false شود، جوجه در برنامه دیده نمی‌شود.
$JOOJE_CONFIG['enabled'] = true;

// تعداد دونه برای «ترک خوردن تخم».
$JOOJE_CONFIG['crack_units'] = 6;

// تعداد دونه برای «تولد». تولد فقط یک‌بار است و هرگز عقب برنمی‌گردد.
$JOOJE_CONFIG['birth_units'] = 12;

// هدف روزانهٔ آب وقتی برنامهٔ جاری فعالیت آب (ACT002) ندارد یا هدفش صفر است.
$JOOJE_CONFIG['daily_water_fallback'] = 0; // ۰ = هدفی اعلام نمی‌شود (تاریخ خالی نمی‌سازیم)

// ساعت‌های «شب» برای حالت خواب (به وقت سرور).
$JOOJE_CONFIG['night_start_hour'] = 22;
$JOOJE_CONFIG['night_end_hour'] = 6;

// غیبت: ۳ تا ۵ روز کم‌رنگ، ۶ روز یا بیشتر خاکستری. جوجه هرگز نمی‌میرد و صفر نمی‌شود.
$JOOJE_CONFIG['faded_days'] = 3;
$JOOJE_CONFIG['gray_days'] = 6;

// سه مرحلهٔ رشد (بعد از تولد). آستانه‌ها از بک‌اند می‌آید، نه فرانت.
$JOOJE_CONFIG['growth'] = array(
    array('key' => 'small',     'label' => 'جوجهٔ کوچک', 'from_units' => 12),
    array('key' => 'fledgling', 'label' => 'نوپا',       'from_units' => 40),
    array('key' => 'adult',     'label' => 'بالغ',       'from_units' => 90),
);
