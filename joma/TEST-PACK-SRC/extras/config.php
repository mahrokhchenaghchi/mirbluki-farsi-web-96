<?php
/**
 * JOMA — کپی تست (joma.mirbolouki.com/test)
 * =========================================================================
 * این کپی کاملاً جدا از سایت اصلی است:
 *   · با فایل کار می‌کند (storage = file) و به هیچ دیتابیسی وصل نمی‌شود.
 *   · داده‌ها فقط داخل پوشهٔ data همین کپی می‌مانند.
 *   · فایل config.php سایت اصلی را با این فایل جایگزین نکن.
 * =========================================================================
 */

$JOMA_CONFIG = array(
    // هیچ دیتابیسی وصل نمی‌شود (خالی بماند)
    'db_host' => '',
    'db_name' => '',
    'db_user' => '',
    'db_pass' => '',

    // آدرس کپی تست؛ اگر خودکار درست تشخیص داده نشد، همین را عوض کن
    'base_url' => '/test',

    // داده در فایل (بدون دیتابیس)
    'storage' => 'file',

    // قفل ثبت‌نام (مثل سایت اصلی): برای ساخت حساب باید کد امنیتی وارد شود.
    'registration_security_code_enabled' => true,
    'registration_security_code' => 'J0m@14O5',
);
