۱۲ — این بسته چیست، چه چیزی عوض شد، چه چیزی عوض نشد
=====================================================================
نام بسته: JOMA-TEST-PACK-v10.zip
مبنا: v8 (آخرین نسخهٔ نصب‌شدهٔ سرور تست) + همهٔ کارهای v9 + کارهای این دور.
**فقط همین یک زیپ را نصب کن.** لازم نیست اول v9 را نصب کنی.
سایت اصلی دست‌نخورده می‌ماند.

──────────────────────────────────────────────
۱) نصب (۳ دقیقه · بدون SQL)
──────────────────────────────────────────────
۱) cPanel → File Manager → پوشهٔ ساب‌دامین joma.mirbolouki.com
۲) پشتیبان: راست‌کلیک روی پوشهٔ test → Compress (اگر خواستی برگردانی).
۳) JOMA-TEST-PACK-v10.zip را Upload کن (کنار پوشهٔ test).
۴) راست‌کلیک روی zip → Extract → «Extract File» → فایل‌های داخل test به‌روز می‌شوند.
۵) مرورگر: https://joma.mirbolouki.com/test/index.php?p=login
   یک‌بار Ctrl+Shift+R بزن (شیت استایل تازه).
۶) تمام. هیچ نصب، هیچ ری‌استارت.

داده‌ات: دست‌نخورده. این بسته با خودش داده نمی‌آورد (اگر خواستی، _SEED-DEMO.php را یک‌بار باز کن).
اگر نصب MySQL داری: یک فایل اختیاری هست — `اختیاری-2-hammasir_invite_codes.sql`
(فقط CREATE TABLE IF NOT EXISTS، برای مسیر «کد دعوت دارم»). در حالت فایل لازم نیست.

──────────────────────────────────────────────
۲) حساب‌های نمونه (برای آزمون همین بسته)
──────────────────────────────────────────────
اگر می‌خواهی دادهٔ آزمون این دور را ببینی، این دو فایل را یک‌بار در مرورگر باز کن
(بعدشان پاک کن):
  · test/tests/seed-c-round.php  → کاربران tana (تک‌نقشه) · tab · tala (روز اول) · hamkar (چندنقشه) + پنج مراجع
  · test/tests/seed-v10.php      → ostad (چندنقشه) · s1 (تک‌نقشه) · modir (مدیر)
                                   و سه مراجع با **۰ / ۴ / ۹ روز بی‌ثبت** + تازه‌وارد + نشانگر خاموش
                                   + یک کد دعوت فعال
رمز همهٔ حساب‌های آزمون: `JomaTest1405!`

چه چیزی را کجا ببینی:
  · C5 (وضعیت در یک نگاه): با ostad وارد شو → نقش «مشاور» → داشبورد مشاور.
    کارت سبز (۰ روز) · طلایی (۴ روز) · کورال (۹ روز) · تخم (تازه‌وارد) · خاکستری‌نشده (نشانگر خاموش).
  · کد دعوت: در همان داشبورد، بلوک «مراجع جدید با کد دعوت» → «ساخت کد دعوت».
    بعد با s1 وارد شو → هم‌مسیر → «کد دعوت دارم» → کد را وارد کن.
  · F1 (حلقهٔ پیشرفت): با s1 وارد شو → «خانهٔ من» → کارت «قدم بعدی».
  · F3 (چگالی نمایش): تنظیمات → بلوک «نمایش» → دو کارت «راحت / فشرده» (کلیک = همان‌لحظه).

──────────────────────────────────────────────
۳) چه چیزی در این بسته **عوض شد**
──────────────────────────────────────────────
این دور (v10):
  ✅ C5 — داشبورد مشاور «وضعیت در یک نگاه»: نوار ۴px رنگی · چیپ متنی · عدد درشت روزهای بی‌ثبت ·
         «آخرین ثبت» · جوجهٔ چهار حالت · ترتیب تازه · فیلتر و سرصفحه با نقطهٔ رنگی
  ✅ کد دعوت یک‌بارمصرف — جدول + ساخت (مشاور) + پذیرش (کاربر) + «کی با کد آمد» + سقف ۵ کد · ۷ روز
  ✅ F1 — حلقهٔ پیشرفت `.mring` در کارت «قدم بعدی» خانه (فقط با عدد واقعی)
  ✅ F2 — پوشش `aria-label`: هفت فیلد بی‌نام نام گرفت؛ آزمون: هیچ دکمه/فیلد بی‌نام نماند
  ✅ F3 — نمونهٔ زندهٔ «چگالی نمایش» در تنظیمات (دو کارت، تغییر همان‌لحظه)

از بستهٔ v9 (که نصب نکرده بودی، این‌جا داخلش هست):
  ✅ C1 — سه تب «روزانه · هفتگی · ماهانه» در کارهای امروز + شمارش + تب خالی با دلیل
  ✅ C2 — جابه‌جایی نقش (فقط چندنقشه) + کنار رفتن سه صفحهٔ شخصی در حالت مشاور + پیام آدرس مستقیم
  ✅ C3 — داشبورد مشاور: مراجعان من · فیلتر · کارت مراجع · رنگ نیلی
  ✅ C4 — کارت دعوت یک‌باره + چهار گام دعوت (بدون تیک پیش‌فرض)

──────────────────────────────────────────────
۴) چه چیزی **عوض نشد** (دست‌نخورده)
──────────────────────────────────────────────
  · همهٔ L01…L33 (فهرست کامل: LOCK-LIST-v8.txt) — بدون تغییر
  · ویزارد «حال من» و پنج تصویرش · لیوان آب و قاعدهٔ یک لمس/یک گذار · تقویم «مسیر این ماه»
  · جمله‌های کاربر (بدون سرزنش · فقط خودت · همراهی، دلیل نیست · داده کافی نیست)
  · کتابخانه · آموزش · گزارش‌ها و نمودارها · دفترچه و بینش · حقوق داده · کنسول مدیر
  · دو تم (کلاسیک/شیشه) و انتخاب تم · جوجه و صدایش · تنفس ۴-۷-۸ · بادکنک استرس
  · config/config.php · داده‌های کاربران · دیتابیس (هیچ UPDATE/DELETE/ALTER)
  · سایت اصلی: هیچ فایلی از آن لمس نشد.

──────────────────────────────────────────────
۵) دو تغییر رفتاری که در C5 اجتناب‌ناپذیر بود (صریح)
──────────────────────────────────────────────
۱) متن حالت «نشانگر خاموش» از «روشن نشده است» به **«وضعیت اشتراک گذاشته نشده»** رفت
   (متن خودت در C5). هیچ چیز دیگری از آن کارت عوض نشد.
۲) متن حالت «آرام» دقیقاً **«۳ تا ۵ روز بی‌ثبت»** شد (متن خودت در C5).
   برای «نیازمند توجه»، عدد واقعی همان مراجع نوشته می‌شود (مثلاً «۹ روز بی‌ثبت») و آستانهٔ شروع ۶ است.

──────────────────────────────────────────────
۶) پنج قلم اثبات (بخش د)
──────────────────────────────────────────────
۱) `LOCK-v8.sha256` — هش SHA-256 همهٔ ۱۲۶ فایل v8 (بی‌تغییر از دور قبل).
۲) `DIFF.txt` — کل اختلاف با v8، با این جمله در سرش:
   «این بسته تجمعی از v8 است؛ به‌تنهایی نصب می‌شود و نصب v9 لازم نیست.»
۳) `LOCK-LIST-v8.txt` — تأیید **L01…L33 ✅ بدون تغییر** و توضیح چرا فایل‌هایی باز شدند.
۴) `11-گزارش-C5-و-F-v10.txt` — گزارش `file:line` برای C5 · کد دعوت · F1 · F2 · F3 · F4.
۵) همان سند، بند ۷ — خروجی آزمون‌ها (اجرا روی خودِ همین زیپ).

بررسی خودت (اختیاری):
     cd test
     sha256sum -c LOCK-v8.sha256
   نتیجهٔ درست: ۱۱۴ خط OK و ۱۲ خط FAILED — همان ۱۲ فایلی که در DIFF.txt نوشته شده.
   (پوشهٔ data حساب نمی‌شود؛ دادهٔ خودت است.)

──────────────────────────────────────────────
۷) وضعیت فایل‌های آزمون و سلامت
──────────────────────────────────────────────
· `health.php` و `tests/*` تا پایان تست می‌مانند؛ بعد از «تأیید است» پاک می‌شوند و
  روی سایت اصلی هیچ‌وقت نمی‌روند (طبق تصمیم خودت).
· دو پرسش باز فایل `03-نقشه-سایت-اصلی.txt` (حالت فایل یا MySQL · بازهٔ انتشار)
  دست‌نخورده مانده و منتظر جواب توست.

──────────────────────────────────────────────
۸) سه پرسش من
──────────────────────────────────────────────
۱) در «نیازمند توجه»، عدد واقعی هر مراجع نوشته می‌شود («۹ روز بی‌ثبت»). اگر متن ثابت
   «۶ روز بی‌ثبت» می‌خواهی، بگو (یک خط است).
۲) با کد دعوت، ارتباط بعد از تأیید کاربر مستقیم «فعال» می‌شود (رضایت مشاور = ساخت کد).
   اگر می‌خواهی مشاور یک تأیید دوم هم بزند، بگو.
۳) محل بلوک کد دعوت در داشبورد مشاور (زیر فهرست مراجعان) درست است یا جای دیگری می‌خواهی؟

راهنماهای قبلی (۷ تا ۱۰) برای مرجع مانده‌اند؛ این فایل، راهنمای نسخهٔ نهایی است.
