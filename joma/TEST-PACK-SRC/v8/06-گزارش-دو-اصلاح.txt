۰۶ — دو اصلاح این بسته (v8) — گزارش با شاهد
=====================================================================

مورد ۱ — «حال من»: هر پنج گزینه تصویر خودش را دارد
---------------------------------------------------------------------
ریشهٔ ایراد: تصویر یک‌بار **بالای هر قدم** رندر می‌شد (یک تصویر بزرگ که با انتخاب عوض می‌شد)
و گزینه‌ها فقط ایموجی/متن داشتند. الان تصویر **داخل خودِ گزینه** است و
**تصویر دو گزینهٔ بغل‌هم یکسان نیست**.

| شاخص | تصویر هر گزینه | فایل:خط | مشخصات سند |
|---|---|---|---|
| حال (general) | پنج چهرهٔ متفاوت (چشم و دهان هر سطح جدا) | `includes/v2_mood.php:127` (`joma_mood_svg_face`) | `SPEC/13 §۳ قدم ۱` |
| انرژی | باتری با پرشدگی ۶/۱۲/۱۸/۲۴/۳۰ و رنگ کورال،کورال،طلایی،آسمانی،نعنایی | `includes/v2_mood.php:44` (`joma_mood_svg_battery`) | «پرشدگی با عرض [6,12,18,24,30] · رنگ ۱-۲ کورال ۳ طلایی ۴ آسمانی ۵ نعنایی» |
| تمرکز | حلقه‌های هدف؛ مرکز با شعاع `1.5+l×1.5` + چهار خط نشانه | `includes/v2_mood.php:60` (`joma_mood_svg_focus`) | «دو حلقه + مرکز شعاع 1.5+l×1.5 + چهار خط نشانه» |
| خواب دیشب | ماه `--lav` + ۱ تا ۵ ستارهٔ طلایی | `includes/v2_mood.php:75` (`joma_mood_svg_sleep`) | «ماه --lav + از ۱ تا ۵ ستارهٔ طلایی» |
| استرس | بادکنک با شعاع `5+(l−1)×2.4` · رنگ آسمانی،آسمانی،طلایی،کورال،رز · بند منحنی · از سطح ۴ دو قوس هوا | `includes/v2_mood.php:92` (`joma_mood_svg_balloon`) | «شعاع ۵+(سطح−۱)×۲٫۴ · رنگ … · بند منحنی · از سطح ۴ قوس هوا» |
| محل درج داخل گزینه | `pages/mood.php:106` (`<span class="wpic">` داخل هر `label.wopt`) | — | — |
| نگاشت شاخص → تصویر | `pages/mood.php:32` (`$imap`) | — | — |
| حذف تصویر بزرگ بالای صفحه | `pages/mood.php:88` (بلوک `.wvisual` حذف شد ⇒ `wvisual = 0`) | — | — |

**شاهد ساختاری (خروجی واقعی صفحه، طبق درخواست تو):**

| قدم | تصویر شمرده‌شده در DOM | گزینه | تصاویر یکتا (هش) |
|---|---|---|---|
| ۱ (حال) | **۵** | ۵ | ۵ ✅ |
| ۲ (انرژی) | **۵** | ۵ | ۵ ✅ |
| ۳ (تمرکز) | **۵** | ۵ | ۵ ✅ |
| ۴ (خواب دیشب) | **۵** | ۵ | ۵ ✅ |
| ۵ (استرس) | **۵** | ۵ | ۵ ✅ |
| ۶ (جملهٔ اختیاری) | ۱ (جغد cheer، طبق بستهٔ v7) | — | — |

`wvisual` (تصویر بزرگ بالای صفحه) = **۰** — حذف شد.

مقادیر خوانده‌شده از HTML واقعی:
· باتری: عرض پرشدگی `6 · 12 · 18 · 24 · 30` · رنگ `coral · coral · gold · sky · ok` ✅
· تمرکز: شعاع مرکز `6.6 · 9.9 · 13.2 · 16.5 · 19.8` (= `(1.5+l×1.5)×2.2`) ✅
· خواب: ستاره‌ها `۱ · ۲ · ۳ · ۴ · ۵` ✅
· استرس: شعاع بادکنک `۱۰ · ۱۴٫۸ · ۱۹٫۶ · ۲۴٫۴ · ۲۹٫۲` (= `5+(l−1)×2.4` ×۲) · رنگ `sky · sky · gold · coral · rose` · قوس هوا در سطح ۴ و ۵ (۲ قوس) ✅
· برچسب‌های استرس: «خیلی کم · کم · متوسط · زیاد · خیلی زیاد» ✅

مورد ۲ — لیوان آب: «هر لمس = یک گذار»
---------------------------------------------------------------------
**ریشهٔ واقعی ایراد (مهم‌تر از سه گمان):** در بستهٔ v7 صفت‌های `data-pa/data-date/data-value/data-target`
به یک `<span>` **داخلی** منتقل شده بودند، در حالی که جاوااسکریپت آن‌ها را از **خود کارت** می‌خواند.
نتیجه: `N` همیشه صفر خوانده می‌شد ⇒
  · با هر کلیک، اول لیوان‌ها «خالی» می‌شدند (چون `showWater(0)`) و بعد پر می‌شدند ⇒
    همان «خودش پر و خالی می‌شود»؛
  · هدف و مقدار به سرور نمی‌رفت (`pa_id` تهی) ⇒ ثبت‌ها بی‌اثر بودند.

**اصلاح:** صفت‌ها به خودِ `<section id="water-card">` برگشتند — `pages/today.php:205-208`.

سه اصلاح دیگری که خواسته بودی، همه پیاده شد (`assets/js/joma-v2.js` بلوک «آب (B6)»):
| خواسته | اجرا | شاهد |
|---|---|---|
| فقط یک رویداد | فقط `click` روی کارت؛ هیچ `pointerdown/up` دیگری | `assets/js/joma-v2.js` بلوک آب — تنها `addEventListener('click'` |
| قفل ضد دوباره‌شلیک ~۴۶۰ms | `busy` + `STEP_MS = 460` | همان بلوک (`var STEP_MS = 460; if (busy) return;`) |
| انیمیشن از N قدیم | `showWater(next)` روی همان DOM؛ CSS فقط `transform` را transition می‌دهد | `assets/css/joma-v2.css` (`.glass .glass-water{transition:transform .46s…}`) |
| SVG بازسازی نشود | گروه `.glass-water` یک‌بار ساخته می‌شود؛ در کلیک فقط `style.transform` عوض می‌شود | `includes/v2_glass.php` (گروه `glass-water`) + JS (`el.querySelector('.glass-water').style.transform`) |
| شرط «لمس دوباره» | `N<k ⇒ k` · `k==N>0 ⇒ k−1` · `k<N ⇒ k` · بدون تغییر ⇒ بازگشت بدون انیمیشن | بلوک JS (همان شرط‌ها) |
| صدای فقط پر شدن | `if (growing) { pour(); tone(); }` | همان بلوک |
| سیلوئت خالی | خط `#BBD8E8` با `stroke-width="0.95"` | `includes/v2_glass.php` |
| سطح آب ۱۳٫۲ زیر لبه | `$surfFull = 18.6` (۵٫۴ + ۱۳٫۲) | `includes/v2_glass.php` |
| دو موج ۳٫۶s و ۵٫۴s | `gw-w1` ۳٫۶s یک‌طرفه · `gw-w2` ۵٫۴s مخالف | `assets/css/joma-v2.css` |
| برق شیشهٔ نازک | دو مسیر ۱٫۷px و ۰٫۹px | `includes/v2_glass.php` |

**آزمون اجباری (اجرا شد — خروجی واقعی):**
روش: `tests/water-tap-logic.mjs` (منطق) و آزمون DOM واقعی با jsdom روی **کد شلیپ‌شده** و
**HTML واقعی سرور** (نه کپی): `node tap-test.mjs` — شامل ارسال واقعی HTTP به سرور.

دنبالهٔ `۱→۱ | ۱→۰ | ۳→۳ | ۱→۱ | ۵→۵ | ۵→۴` (نماد: لیوانِ لمس‌شده → وضعیت پس از یک لمس):

| لمس | قبل | بعد | انتظار | درخواست‌ها | پرش؟ | نتیجه |
|---|---|---|---|---|---|---|
| ۱ | ۰ | ۱ | ۱ | ۱ | ندارد | PASS |
| ۱ | ۱ | ۰ | ۰ | ۱ | ندارد | PASS |
| ۳ | ۰ | ۳ | ۳ | ۱ | ندارد | PASS |
| ۱ | ۳ | ۱ | ۱ | ۱ | ندارد | PASS |
| ۵ | ۱ | ۵ | ۵ | ۱ | ندارد | PASS |
| ۵ | ۵ | ۴ | ۴ | ۱ | ندارد | PASS |

· **کلیک سریع:** پنج کلیک پشت‌سرهم ⇒ فقط **۱** گذار و **۱** درخواست ✅
· **موبایل ۴۱۲px:** همان کد و همان دنباله: `۳→۳ | ۳→۲ | ۱→۱ | ۵→۵` ✅
· **بدون تغییر:** کلیک روی جای خالی کارت ⇒ صفر گذار، صفر درخواست ✅
· **مجموع آزمون‌ها: pass=34 fail=0**

**شاهد سرور (یک لمس = یک رکورد):**
```
رکوردهای آب امروز: ۱ → [('1405-06-27', 2)]        ← با ۱۴ لمس، فقط ۱ رکورد
وضعیت ذخیره‌شده: {'status': 'draft', 'value': 2, …}  ← مقدار = آخرین لمس
```
