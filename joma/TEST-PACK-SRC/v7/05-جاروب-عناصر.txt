۰۵ — جاروب کامل: هر عنصر سند × هست/نیست
=====================================================================
روش: هر ردیف با `grep` روی کد یا با درخواست واقعی به صفحه سنجیده شده است.
«شاهد» یعنی همان جایی که در کد پیدا می‌شود. ردیفی که صفر شمرده، «🔴 نیست» است — حتی اگر کسی نگفته باشد.

الف) عناصر بصری
---------------------------------------------------------------------
| عنصر | وضعیت | شاهد |
|---|---|---|
| باتری (انرژی) | ✅ | `pages/mood.php` (`'svg' => 'battery'`) → `includes/v2_mood.php:12` |
| سیبل/حلقه‌های هدف (تمرکز) | ✅ | `'svg' => 'rings'` · سه حلقهٔ هم‌مرکز با شعاع `(1.5+l×1.5)×2.2` |
| ماه و ستاره (خواب) | ✅ | `'svg' => 'moon'` + ستاره‌ها بر اساس سطح |
| بادکنک (استرس) | ✅ | `'svg' => 'balloon'` (v7 ساخته شد؛ شعاع و رنگ‌های سند) |
| چهره‌ها (حال عمومی) | ✅ | `joma_v2_mood_svg('face', …)` با پنج حالت |
| تخم / تخمِ ترک‌خورده / جوجه | ✅ | `includes/v2_chick.php` (`stage === 'egg' / 'crack' / chick`) |
| پنج حالت جوجه + نوازش | ✅ | CSS: `pet-state-calm/happy/sleep/faded/gray` + `.pet-touch` |
| کاشی تقویم (سه‌بعدی) | ✅ | `includes/v2_calendar.php` + CSS `.cal-tile` (گرادیان + سایه + `aspect-ratio:1`) |
| لیوان تامبلر (آب) | ✅ | `includes/v2_glass.php` (بدنه، لبهٔ نازک، ته ضخیم، موج دو لایه) |
| دایرهٔ تنفس | ✅ | `pages/today.php` (`#breath-circle` ۲۲۰px) |
| نمودار خطی | ✅ | `includes/success_view.php` (`"line"`) |
| نمودار سطحی (area) | ✅ | `includes/success_view.php` (`area` در تنظیمات fill) |
| نمودار ستونی | ✅ | `pages/reports.php` (`type:"bar"`) |
| نمودار دونات | ✅ | `includes/success_view.php` (`doughnut`) |
| نقشهٔ حرارتی | ✅ | `includes/analytics_view.php` (`hm-cell`) |
| نمودار پراکندگی | ✅ | `includes/analytics_reports.php` (`scatter`) |
| میلهٔ افقی | ✅ | `pages/reports.php` (`indexAxis:"y"`) |
| **حلقهٔ پیشرفت (`.mring`)** | 🔴 **نیست** | در CSS و PHP هیچ اثری از `mring` / `stroke-dasharray` نیست. درصدها با نوار و دونات دیده می‌شوند، ولی حلقهٔ پیشرفتِ سند ساخته نشده. |
| آیکون‌ها (اسپرایت) | ✅ | `includes/v2_sprite.php` — ۵۴ symbol |
| لوگو (کاشی جغد) | ✅ | `.logo-tile` + `owl-hi` در همهٔ سرصفحه‌ها |
| استیکر وقت (۴ حالت) | ✅ | `includes/v2_time.php` |

ب) صداها
---------------------------------------------------------------------
| صدا | وضعیت | شاهد |
|---|---|---|
| صدای آب (ریختن + پلینک) | ✅ | `assets/js/joma-v2.js` (WebAudio: `pour()` + `tone()`) · پیش‌فرض خاموش |
| سه جملهٔ تنفس | ✅ | `assets/audio/br-in.mp3` · `br-hold.mp3` · `br-out.mp3` (+ متن همان جمله‌ها زیر دایره) |
| جیک جوجه | ✅ | `assets/js/joma-v2.js` (`chirp()` — ۴۷۰→۷۸۰ و ۶۲۰→۹۰۰، ≈۰٫۲۱s) |
| خرخر | ✅ | `purr()` (مثلثی ۱۱۲→۱۲۶ + ترمولوی ۲۶Hz + لایهٔ نفس) — با دکمهٔ «نوازش» |
| خمیازه | ✅ | `yawn()` (۳۴۰→۱۹۰) |
| خروپف | ✅ | `snoreOnce()` (دم ۰٫۶۲s · بازدم ۰٫۸۵s) + تکرار آرام در حالت خواب |
| بلندی ملایم | ✅ | `MASTER = 0.5` (≈ −۶dB) |
| پیش‌فرض خاموش (هر سه) | ✅ | `joma_water_sound` / `joma_breath_sound` / `joma_pet_sound` = `'1'` برای روشن‌شدن |

ج) رفتارها
---------------------------------------------------------------------
| رفتار | وضعیت | شاهد |
|---|---|---|
| `wiggle` جغد (بی‌پاداش) | ✅ | `pages/dashboard.php` (`#joma-msg`) + JS «جغد راهنما» |
| `popin` تکمیل کارها | ✅ | `pages/today.php` کارت cheer + CSS `.popin` |
| `petGate` (۴ و ۸ لمس) | ✅ | `assets/js/joma-v2.js` (`n >= 4` / `n >= 8`) |
| «بخوابانش» | ✅ | `pages/jooje.php` (`data-pet-sleep`) + `pet-sleeping` + zzz |
| «کاهش حرکت» | ✅ | ۴ بلوک `@media (prefers-reduced-motion: reduce)` در CSS |
| سجاف امن | ✅ | `@supports (padding: env(safe-area-inset-bottom))` برای تب‌بار/محتوا/دیالوگ/سایدبار |
| فوکوس و `aria-label` | 🟡 | ۱۲ مورد `aria-label` (تقویم، جغد، لیوان، رمز، جوجه) + `aria-live` برای نوار خلاصهٔ تقویم. برای همهٔ دکمه‌های آیکونی پوشش کامل نیست. |
| روان بودن (بدون بارگذاری صفحه) | ✅ | آب با `fetch` + پاسخ JSON؛ چند کلیک پشت‌سرهم با محافظ `busy` |
| ترجیحات نمایش (کارت فشرده) | ✅ | `includes/layout.php` (`body.compact`) + CSS تازه (v7) — قبلاً ذخیره می‌شد و بی‌اثر بود |
| انتخاب تم (کلاسیک/شیشه) | ✅ | `pages/settings.php` + `[data-theme]` + اسکریپت ضدپرش |
| ثبت آینده بسته | ✅ | `functions/joma.php` (`ثبت عملکرد برای روزهای آینده ممکن نیست.`) |
| بی‌داده ≠ صفر | ✅ | جوجه، گزارش‌ها، تقویم (بدون ثبت = رنگ آبی‌خاکستری، نه صفر) |

خلاصه: **۴ ردیف 🟡/🔴** — «حلقهٔ پیشرفت» (🔴) و «پوشش کامل aria-label» (🟡)
و دو ردیف توضیحی؛ بقیه ✅. (اگر حلقهٔ پیشرفت را می‌خواهی، ساختش کوچک است و در بستهٔ بعد می‌آید.)
